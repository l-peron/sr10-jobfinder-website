<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.4/jquery.min.js"></script>
    <link rel="stylesheet" href="/stylesheets/fichesemploi_list.css"/>
    <title>WORKYS</title>
  </head>
  <body>
    <%- include('../../error.ejs'); %>
    <div id="main_overlay"></div>
    <script type="module" src="/javascripts/index.js"></script>
    <%- include('../../header.ejs'); %>
    <main class="p-3">
        <h1>Editer l'offre d'emploi</h1>
        <form method="post" action="/recruiter/offresemploi/<%= offre.id %>/edit" class="d-flex flex-column flex-grow-1 p-3">
            <div class="d-flex flex-row">
                <div class="p-1">
                    <h2 class="mx-2">Fiche de poste</h2>
                    <select name="oe_fiche" class="p-1 m-2" value="<%= offre.fiche %>">
                        <% fichesPoste.forEach((fiche)=> { %>
                            <option value="<%= fiche.id %>"> <%= fiche.title %> </option>
                        <% }) %>
                    </select>
                </div>
                <div class="p-1">
                    <h2 class="mx-2">Documents requis</h2>
                    <input type="text" name="oe_required_documents" placeholder="Documents requis" class="p-1 m-2" value="<%= offre.required_documents %>"/>
                </div>
                <div class="p-1">
                    <h2 class="mx-2">Date de fin de validité</h2>
                    <input type="date" name="oe_valid_date" placeholder="Date de début" class="p-1 m-2" value="<%= offre.valid_date %>"/>
                </div>
            </div>
            <div class="p-1">
                <input type="submit" value="Modifier" class="w-100"/>
            </div>
            </form> 
    </main>
    <%- include('../../nav.ejs'); %>
</body>
</html>